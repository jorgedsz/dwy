{
  "name": "done-with-you",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install --prefix client && npm run build --prefix client && npm install --prefix server && npx --prefix server prisma generate --schema=server/prisma/schema.prisma",
    "start": "npx --prefix server prisma migrate deploy --schema=server/prisma/schema.prisma && node server/src/index.js",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client"
  }
}
